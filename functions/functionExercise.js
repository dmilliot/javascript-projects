function makeLine(size) {
    let output = '';
    for (i=0; i<size; i++) {
    output += '#';     
    }
    console.log(output);
}
// makeLine(10);

function makeSquare(size) {
    let output = '';
    for (i=0; i<size; i++) {
        output += '#';
    }
    console.log(output);
}

makeSquare(5);